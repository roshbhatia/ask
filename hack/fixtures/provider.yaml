version: provider/v1
name: recorded-review
description: Screenshot fixture
command: [ask-provider-recorded-review]
actions:
  inference.generate:
    description: Generate a deterministic demonstration
  provider.validate:
    description: Validate the demonstration provider
requires:
  commands: [ask-provider-recorded-review]
defaults:
  timeout: 30s
